# AGENTS.md

This file is for any agent (human or automated) that provisions a dev
environment for **leaderboarder** and starts the API.

The project uses **Leiningen** (installed from the official script) and **Clojure** (via Lein). No system package of
Leiningen is required.

